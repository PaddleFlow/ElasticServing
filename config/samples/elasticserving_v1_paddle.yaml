apiVersion: elasticserving.paddlepaddle.org/v1
kind: Paddle
metadata:
  name: paddle-sample
spec:
  # Add fields here
  resources:
    requests:
      cpu: 80m
  deploymentName: paddle
  replicas: 1
  # storageUri: paddlepaddle/serving:latest
  # runtimeVersion: paddleserving
  storageUri: nginx:latest
  runtimeVersion: nginx